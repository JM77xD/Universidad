#include <iostream>

using namespace std;

int main() {
    const int TAM_MAX = 1000;
    char caracter[TAM_MAX];
    int frecuencia[TAM_MAX], contador;
    for (int i = 0; i < TAM_MAX; i++) frecuencia[i] = 0;
}